# `public/images/`

Binary assets live here. The files were originally at `public/images/` at the
repo root — copy them over with:

```bash
# from repo root
cp -r public/images/* apps/frontend/public/images/
```

Files in this folder before restructure:
- `sabbir-dev.jpeg`